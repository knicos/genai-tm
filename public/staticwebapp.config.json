{
    "routes": [
        {
            "route": "/image*",
            "rewrite": "index.html"
        },
        {
            "route": "/deploy*",
            "rewrite": "index.html"
        }
    ]
}
